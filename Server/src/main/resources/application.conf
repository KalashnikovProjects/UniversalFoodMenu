ktor {
  deployment {
    port = 8080
    port = ${?PORT}
  }
  application {
    modules = [
      com.kalashnikovprojects.SerializationKt.configureSerialization
      com.kalashnikovprojects.SecurityKt.configureSecurity
      com.kalashnikovprojects.ExposedKt.configureExposed
      com.kalashnikovprojects.RoutingKt.configureRouting
    ]
  }
}